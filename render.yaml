services:
  - type: web
    name: govshield-ai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python backend/main.py
    envVars:
      - key: PORT
        value: 10000
      - key: GEMINI_API_KEY
        sync: false
